# input-bindings

Reusable, deterministic input-binding foundation for editors, games, and applications.

The repository is intentionally application-agnostic: consuming projects own semantic actions and defaults; this repository owns binding representation, context-aware resolution, conflict analysis, profiles, and platform adapters.
